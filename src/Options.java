// Mrs. Kelly
// A File to keep track of global constants for the entire projects
import java.awt.Dimension;
import java.awt.Point;

public class Options {

public static Point gameLocation = new Point(200, 100); // Where the JFrame will show up on the screen
public static Dimension gameSize = new Dimension(800, 400); // How big the JFrame will be
public static int gameTime = 45;                        // How man seconds start on the timer
}